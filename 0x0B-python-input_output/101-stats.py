#!/usr/bin/python3
"""
this reads the stdin line by line and computes metrics
"""
import sys

file_size  = 0
status_ tally = {}


j = 0
try:

except KeyboardInterrupt:
 print("File size: {:d}".format(file_size))
print("{:s}: {:d}".format(key, value))
